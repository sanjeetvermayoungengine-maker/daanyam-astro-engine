# Benchmarks

Criterion benchmark sources currently live inside crate-local `benches/` directories so they can participate in Cargo bench runs while this top-level folder tracks benchmark strategy and future shared datasets.

