ASTRO_BACKEND=de440
HOST=0.0.0.0
PORT=3000
ASTRO_EPHE_PATH=/ephe/de440.bsp
RUST_LOG=warn

# Production convention:
# - Mount de440.bsp read-only at /ephe/de440.bsp
# - Docker/VM deployments typically use a bind mount or managed volume
# - Kubernetes deployments should prefer a PVC or equivalent read-only volume
# - Never commit or bake de440.bsp into source control or the container image
